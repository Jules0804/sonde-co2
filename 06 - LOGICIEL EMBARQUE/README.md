# Logiciel embarqué

Ce dossier contient le logiciel de la carte.

Actif maintenant :

- `firmware-esp32/tests-materiel/blink_gpio39` : test LED GPIO39.
- `firmware-esp32/tests-materiel/wifi_ap_web` : Wi-Fi AP + site web embarqué.
- `firmware-esp32` : squelette firmware cible.

Prochaine action : préparer puis tester la lecture SCD41 et la sortie DFR0971 quand les modules arrivent.

